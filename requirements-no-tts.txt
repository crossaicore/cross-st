# requirements-no-tts.txt — Cross without TTS/audio packages
#
# Use this file if you do not need st-speak, st-voice, or --mp3 rendering.
# TTS packages (soundfile, yakyak, websockets) have wheels for Python 3.10–3.13
# on macOS ARM — there is no Python version reason to avoid them.  Use this
# file if you simply do not want audio support, or need a lighter install.
#
# PYTHON VERSION: 3.10, 3.11, 3.12, or 3.13
#
#   MINIMUM 3.10 — still required (numpy 2.x, scipy 1.15.x, match statements)
#
# Commands unavailable without TTS packages:
#   st-speak, st-voice, st-prep --mp3 / --all
# All other commands (st-gen, st-cross, st-stones, st-fact, st-post …) work normally.
#
# Install:
#   python3.11 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements-no-tts.txt
#   bash script/symbolic_links.bash
#
# For full TTS support use requirements.txt with Python 3.10–3.13.

# ── AI providers ──────────────────────────────────────────────────────────────
cross-ai-core[all]==0.11.0     # AI provider stack (anthropic, openai, gemini, xai, perplexity; + ollama local/LAN)
# anthropic/google-genai/openai are transitive deps of cross-ai-core; re-pinned
# here explicitly so pip-audit and Dependabot can scan exact developer versions.
anthropic==0.92.0
google-genai==1.71.0
openai==2.31.0

# ── Discourse posting ─────────────────────────────────────────────────────────
pydiscourse==1.7.0
requests>=2.32.4

# ── Data analysis and plotting ────────────────────────────────────────────────
matplotlib==3.10.1
numpy==2.2.4
pandas==2.2.3
pillow>=12.1.1
scipy==1.15.2
seaborn==0.13.2

# ── Text processing ───────────────────────────────────────────────────────────
markdown==3.7
tabulate==0.9.0
textstat==0.7.13
tqdm==4.67.1
weasyprint==68.1             # st-print.py — Markdown → PDF (no TTS dependency)

# ── Environment and utilities ─────────────────────────────────────────────────
python-dotenv==1.1.0
setuptools>=78.1.1
urllib3>=2.6.3

# TTS/audio packages (cmudict, pyphen, soundfile, websockets, wyoming, yakyak)
# are intentionally omitted.  st-speak, st-voice, and st-prep --mp3 will print
# an installation hint and exit cleanly if invoked.
